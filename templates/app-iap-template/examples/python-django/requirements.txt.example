# Minimal Django + Postgres + gunicorn. Copy → requirements.txt, thêm libs theo app.
Django==5.1.2
gunicorn==23.0.0
psycopg[binary]==3.2.3

# Optional thường dùng:
# django-environ==0.11.2
# djangorestframework==3.15.2
# whitenoise==6.7.0          # serve static qua app nếu không dùng Traefik static
