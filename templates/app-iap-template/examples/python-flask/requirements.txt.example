# Minimal Flask + gunicorn. Copy → requirements.txt, thêm libs theo app.
Flask==3.0.3
gunicorn==23.0.0

# Optional thường dùng:
# python-dotenv==1.0.1
# flask-sqlalchemy==3.1.1
# psycopg[binary]==3.2.3
# requests==2.32.3
